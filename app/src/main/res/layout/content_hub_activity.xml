<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/app_bar_hub_activity">

    <fragment
        android:layout_marginTop="-66dp"
        android:id="@+id/nav_host_fragment_content_hub_activity"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:navGraph="@navigation/mobile_navigation" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/nav_view_bottom"
        android:layout_width="match_parent"
        style="@style/toolbar.default"
        android:layout_gravity="bottom"
        app:itemIconTint="?iconColor"
        app:itemTextColor="?iconColor"
        app:menu="@menu/activity_main_bottom" />


</FrameLayout>